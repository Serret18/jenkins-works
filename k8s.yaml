apiVersion: apps/v1
kind: Deployment
metadata:
  name: jenkins-test-ks
  namespace: default
spec:
  selector:
    matchLabels:
      app: jenkins-test-ks
  template:
    metadata:
      labels:
        app: jenkins-test-ks
    spec:
      containers:
      - image: swr.na-mexico-1.myhuaweicloud.com/test-swr/jenkins-test-ks:<BUILD_TAG>
        imagePullPolicy: IfNotPresent
        command:
          - sleep
        args:
          - '1000'
        name: jenkins-test-ks
        env:
        - name: branch
      imagePullSecrets:
        - name: default-secret
